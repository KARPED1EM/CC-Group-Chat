{
  "name": "cc-group-chat",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "bun test",
    "build": "bun build packages/channel/src/server.ts --outfile bin/channel.js --target bun && bun build packages/broker/src/daemon.ts --outfile bin/broker.js --target bun"
  },
  "devDependencies": {
    "@types/bun": "1.3.13",
    "bun-types": "1.3.13",
    "typescript": "5.9.3"
  }
}
