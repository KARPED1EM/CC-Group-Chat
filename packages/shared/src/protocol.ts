// Wire protocol between the channel server (per-session) and the broker daemon.
// Populated when the IPC layer lands; reserved here so consumers can import
// the file path without breakage during incremental development.
export {}
